import React from 'react';
import Feed from '../Feed';

import { Container } from './styles';

const Main: React.FC = () => {
    return (
        <Container>
            <Feed />
        </Container>
    );
};

export default Main;
